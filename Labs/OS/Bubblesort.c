#include<stdio.h>

int bubble(int *a, int num){
    
}
int main(void){
    int a[10] = {0,2,3,4,5,1,2,3,4,10};
    int num;
    int check;
    printf("Enter a number: ");
    scanf("%d", &num);
    if(check = bubble(a, num)){
        printf("Number Found at %d", check);
    }
    return 0;
}